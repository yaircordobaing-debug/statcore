import { defineConfig } from 'vite';

export default defineConfig({
  // Configuración base vital para que GitHub Pages encuentre el CSS y los assets.
  // Debe coincidir con el nombre de tu repositorio.
  base: '/statcore/'
});
